/**
 * @deprecated — React visual registry lives in @ice/ui/features/concepts.
 * The data-only family registry is in ./types.ts as `registerConceptFamily`.
 *
 * This file is kept as an empty stub so any stray imports don't break the
 * build while Slice 2 restructures the UI layer. Delete when Slice 2 lands.
 */

export {};
